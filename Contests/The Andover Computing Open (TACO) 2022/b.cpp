#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(0); cin.tie(NULL); cout.tie(NULL);
    int x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;
    if (x1 < 0) x1 *= -1;
    if (y1 < 0) y1 *= -1;
    if (x2 < 0) x2 *= -1;
    if (y2 < 0) y2 *= -1;
    if (abs(x1 - y1) == abs(x2 - y2)){
        
    }
    return 0;
}