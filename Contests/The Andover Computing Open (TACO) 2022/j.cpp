#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){
    ios_base::sync_with_stdio(0); cin.tie(NULL); cout.tie(NULL);
    int n;
    cin >> n;
    vector<int> grpA(n), grpB(n);
    for (int i = 0; i < n; i++){
        cin >> grpA[i];
    }
    for (int i = 0; i < n; i++){
        cin >> grpB[i];
    }
    for (int i = 0; i < n; i++){
        
    }
    
    return 0;
}